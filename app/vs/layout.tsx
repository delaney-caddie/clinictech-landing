import "./vs.css";

// Shared stylesheet for the /vs comparison pages; page chrome (nav/footer)
// stays in each page so they match the rest of the site.
export default function VsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
